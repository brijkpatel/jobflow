# Build Authentication System

## Overview
Multi-step implementation of JWT-based auth.

### Subtasks
1. Setup JWT utilities
2. Add login endpoint (depends on: Setup JWT utilities)
3. Add token refresh endpoint (depends on: Add login endpoint)
4. Add logout endpoint (depends on: Setup JWT utilities)
5. Integration tests (depends on: Add login endpoint, Add logout endpoint)
